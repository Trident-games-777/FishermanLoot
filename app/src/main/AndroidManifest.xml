<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.zzkkcom.roblox.clien">

    <application
        android:name=".app.FishmanApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_fishman"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_fishman_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FishermanLoot"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_id" />
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_token" />

        <activity
            android:name=".app.FishmanActivity2"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".sea.SeaActivity"
            android:exported="false" />

        <activity
            android:name=".app.FishmanActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>